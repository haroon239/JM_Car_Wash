import { DashboardController } from "../features/dashboard/DashboardController";

/** Root composition component. Feature behavior lives in dedicated modules. */
export default function App() {
  return <DashboardController />;
}
